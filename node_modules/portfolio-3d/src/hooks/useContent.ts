import { useState, useEffect } from 'react';
import { contentAPI } from '../services/contentAPI';
import { portfolioData } from '../data/content';

interface Config {
  contactEmail?: string;
  heroTitle?: string;
  heroSubtitle?: string;
  phone?: string;
  location?: string;
}

interface Sections {
  [key: string]: string;
}

export const useContent = () => {
  const config = portfolioData.config;
  const sections = null; // Sections can be null or we could parse them from portfolioData if needed
  const error = null;

  return { config, sections, error };
};
